export enum OrderStatus {
  ORDERED = '0',
  DELIVERED = '1',
  COMPLETE = '2',
}

export enum PaymentStatus {
  PRE_PAID = '0',
  ACTIVE = '1',
  INACTIVE = '2',
}

export enum DeliveryType {
  BY_COURIER = '0',
  BY_BUS = '1',
  OTHER = '2',
}
